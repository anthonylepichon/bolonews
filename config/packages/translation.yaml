# Présentation : configuration de la traduction Symfony.
# Rôle : fixer la langue par défaut et les chemins des éventuels messages traduits.
framework:
    default_locale: en
    translator:
        default_path: '%kernel.project_dir%/translations'
        providers:
