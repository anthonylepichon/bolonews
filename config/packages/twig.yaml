# Présentation : configuration du moteur de templates Twig.
# Rôle : identifier les fichiers de vues et renforcer leurs contrôles dans l'environnement de test.
twig:
    file_name_pattern: '*.twig'

when@test:
    twig:
        strict_variables: true
